name: Agent instruction hierarchy

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: canonical-cloud/canonical.cloud/.github/workflows/agents-hierarchy.yml@b0307b4d178b9e0a2579a46f581a0cb1fab78d0e
