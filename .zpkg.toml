[package]
org = "canonical-cloud"
name = "canonical-interfaces"
version = "0.1.0"
kind = "contracts"
description = "Canonical Cloud API, compliance, quote, pre-interest, sync, SQL, and generated language contracts"
license = "MIT"
keywords = ["interfaces", "compliance", "quote", "pre-interest", "json-schema", "typespec", "protobuf", "sync", "polyglot"]

[package.repository]
vcs = "git"
url = "https://github.com/canonical-cloud/canonical-interfaces"

[publish]
include_readme = true
tag_format = "v{version}"
smoke_test = "sh \"$ZED_PKG_TEST_TARGET/scripts/tests/smoke-published-artifact.sh\""
exclude = [".env", ".env.*", ".direnv/**", ".zed/**", ".zed-pack/**", "tmp/**", "**/*.log", ".DS_Store", "**/target/**", "**/node_modules/**"]

[targets.repository]
dir = "."
name = "canonical-interfaces"

[targets.typescript]
dir = "generated/typescript"
adapter = "node"

[targets.rust]
dir = "generated/rust"
adapter = "rust"

[targets.rust-wasm]
dir = "generated/rust-wasm"
adapter = "node"
ecosystem = "npm"

[targets.dart]
dir = "generated/dart"
adapter = "dart"

[targets.python]
dir = "generated/python"
adapter = "python"

[targets.golang]
dir = "generated/go"
adapter = "go"

[scripts]
test = "npm test"
