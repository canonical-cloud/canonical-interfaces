{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CanonicalCompliance",
  "description": "Domain types for canonical.plus compliance-readiness engagements. Boundary note: canonical.plus prepares clients for independent review only; formal determinations (audits, attestations, certifications, authorizations) are made by independent auditors, assessors, and regulators.",
  "$defs": {
    "AuditEngagement": {
      "type": "object",
      "description": "A single readiness engagement preparing a customer company for independent review against a framework.",
      "required": ["id", "company", "framework", "status", "opened_at"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Opaque engagement identifier (UUID)."
        },
        "company": {
          "type": "string",
          "description": "Customer company name."
        },
        "framework": {
          "type": "string",
          "enum": ["soc2", "fedramp", "hipaa", "iso_27001", "pci_dss", "gdpr", "cis_controls", "cmmc", "csa_ccm", "dora", "iso_22301", "iso_27701", "nis2", "nist_csf", "nist_800_53"],
          "description": "Framework the readiness engagement prepares the client for."
        },
        "status": {
          "type": "string",
          "enum": ["scoping", "remediation", "audit_ready", "in_audit", "complete"],
          "description": "Lifecycle stage of the readiness engagement. audit_ready means preparation is complete and the client is ready to enter independent review; in_audit means the independent auditor's review is underway."
        },
        "opened_at": {
          "type": "string",
          "description": "RFC 3339 timestamp the engagement was opened."
        },
        "target_report_date": {
          "type": "string",
          "description": "Optional RFC 3339 date the client targets for the independent auditor's report."
        }
      }
    }
  }
}
